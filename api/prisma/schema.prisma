generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("dbhost")
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
}